package com.example.gitgrub;

import java.sql.Connection;
import java.sql.PreparedStatement;
/*



DELETE THIS PAGE BEFORE WE SUBMIT THE PROJECT FOR THE LOVE OF GOD



 */
public class JavaforDummies {
//Insert sql query
//    final String SELECT_QUERY = "SELECT * FROM users WHERE user_id = ? and user_password = ?";
//test db connection
//        try (
//    Connection connection = DBConn.connectDB()) {
//        assert connection != null;
//    Check query
//        try (PreparedStatement preparedStatement = connection.prepareStatement(SELECT_QUERY)) {
//            Check the result
//            if (resultSet.next()) {


















//            }
//        }
//    }
    //SQL exception always include in catch regardless idk why we do this its just some shit we have to do
//} catch (SQLException
//        printSQLException
//        }
//        return false;
}
